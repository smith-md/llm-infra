# agents

Agent implementations, orchestrators, and runtime code.

Ideas:
- `runners/` for different runtime modes (sync, async, server)
- `policies/` for agent behaviour
- `examples/` with simple agent implementations

---
