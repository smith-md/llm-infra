# project

Per-project bootstrapping and memory/backing-store components.

Subfolders:
- `boot/` — project bootstrap scripts, scaffolding, templates
- `memory/` — memory store implementations and connectors (vector DB, SQL, caches)

Suggested next files:
- `boot/init.sh` or `boot/README.md` (explains boot steps)
- `memory/README.md` (details memory choices)

---
